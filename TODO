Use public hostname for full hostname during reconfig
else run:
hostname short_hostname &&  rocks add host alias short FQDN && rocks sync config


Fix proxy in yum.conf
Change root password, re-generate ssh keys
NTP, time zone
Decide managed/unmanaged and kickstartable appliances
Remove v00 from hosts after fixing 
virsh austostart VM on the host
Set resolv.conf correctly

#NOT needed
Fix root directory permissions
inittab
Install ipaddress package in standard and rocks python
Modify DB with the SQL
